// js/global-utils.js
import {
  el,
  log,
  getUrlParam,
  formatNumber,
  formatDateTime,
  getKommuneNavn,
  getValgstedNavn,
  getValgstederForKommune,
  farve,
} from "../index.js";

// Eksportér til window
window.el = el;
window.log = log;
window.getUrlParam = getUrlParam;
window.formatNumber = formatNumber;
window.formatDateTime = formatDateTime;
window.getKommuneNavn = getKommuneNavn;
window.getValgstedNavn = getValgstedNavn;
window.getValgstederForKommune = getValgstederForKommune;
window.farve = farve;
